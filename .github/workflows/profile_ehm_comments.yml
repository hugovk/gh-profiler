# # Troubleshooting workflow to profile me on every comment I make, to sort out workflow
# # that profiles the author of each new issue and PR.

# on:
#   issue_comment:
#     types: [created]

# permissions:
#   issues: write
#   pull-requests: write

# jobs:
#   reply:
#     if: ${{ github.event.comment.user.login == 'ehmatthes' }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: astral-sh/setup-uv@v6

#       - name: Run gh-profiler
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           uvx gh-profiler@latest ehmatthes --concise > profiler.txt
#           echo "=== gh-profiler output ==="
#           cat profiler.txt

#       - name: Comment on issue or PR
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           {
#             echo "Profile summary:"
#             echo
#             echo '```text'
#             cat profiler.txt
#             echo '```'
#           } > body.txt

#           jq -Rs '{body: .}' body.txt > payload.json

#           gh api \
#             repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
#             --input payload.json